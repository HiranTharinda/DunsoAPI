meta {
  name: auth/login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email":"hirantharinda1@gmail.com",
    "password":"abcdef"
  }
}

script:post-response {
  let data = res.getBody();
  bru.setEnvVar("auth_token",data.token);
}
